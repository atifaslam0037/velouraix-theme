{%- comment -%}
  Block: group
  Path: blocks/group.liquid
  Description: Reusable group container block for spacing and alignment.
               Accepts ALL @theme blocks and @app blocks as children.
{%- endcomment -%}

{% stylesheet %}
  .block-group {
    display: flex;
    position: relative;
    overflow: hidden;
  }

  .block-group--vertical {
    flex-direction: column;
  }
  .block-group--horizontal {
    flex-direction: row;
    flex-wrap: wrap;
  }

  .block-group--align-left {
    align-items: flex-start;
  }
  .block-group--align-center {
    align-items: center;
  }
  .block-group--align-right {
    align-items: flex-end;
  }

  .block-group--justify-top {
    justify-content: flex-start;
  }
  .block-group--justify-center {
    justify-content: center;
  }
  .block-group--justify-bottom {
    justify-content: flex-end;
  }
  .block-group--justify-space-between {
    justify-content: space-between;
  }

  .block-group--width-fit {
    width: fit-content;
  }
  .block-group--width-fill {
    width: 100%;
  }

  .block-group--height-fit {
    height: fit-content;
  }
  .block-group--height-fill {
    height: 100%;
  }

  .block-group--border-none {
    border: none;
  }
  .block-group--border-solid {
    border: 1px solid var(--block-group-border-color, currentColor);
  }

  .block-group__bg-media {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
    pointer-events: none;
  }

  .block-group__overlay {
    position: absolute;
    inset: 0;
    z-index: 1;
    pointer-events: none;
  }

  .block-group__inner {
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: inherit;
    align-items: inherit;
    justify-content: inherit;
    gap: inherit;
    width: 100%;
  }

  .block-group__link {
    position: absolute;
    inset: 0;
    z-index: 3;
    display: block;
  }

  @media (max-width: 749px) {
    .block-group--mobile-width-fit {
      width: fit-content;
    }
    .block-group--mobile-width-fill {
      width: 100%;
    }
  }
{% endstylesheet %}

{%- liquid
  assign has_bg_image = false
  assign has_overlay = false
  assign has_link = false

  if block.settings.bg_media_type == 'image' and block.settings.bg_image != blank
    assign has_bg_image = true
  endif

  if block.settings.overlay_opacity > 0
    assign has_overlay = true
  endif

  if block.settings.block_link != blank
    assign has_link = true
  endif

  assign width_style = ''
  assign height_style = ''

  if block.settings.width == 'custom'
    assign width_style = 'width: ' | append: block.settings.custom_width | append: 'px;'
  endif

  if block.settings.height == 'custom'
    assign height_style = 'height: ' | append: block.settings.custom_height | append: 'px;'
  endif

  assign overlay_opacity = block.settings.overlay_opacity | divided_by: 100.0
-%}

<div
  class="
    block-group
    block-group--{{ block.settings.direction }}
    block-group--align-{{ block.settings.alignment }}
    block-group--justify-{{ block.settings.position }}
    block-group--border-{{ block.settings.border }}
    block-group--width-{{ block.settings.width }}
    block-group--height-{{ block.settings.height }}
    block-group--mobile-width-{{ block.settings.mobile_width }}
    color-{{ block.settings.color_scheme }}
  "
  style="
    gap: {{ block.settings.gap }}px;
    padding-top: {{ block.settings.padding_top }}px;
    padding-bottom: {{ block.settings.padding_bottom }}px;
    padding-left: {{ block.settings.padding_left }}px;
    padding-right: {{ block.settings.padding_right }}px;
    border-radius: {{ block.settings.corner_radius }}px;
    --block-group-border-color: {{ block.settings.border_color }};
    {{ width_style }}
    {{ height_style }}
  "
  {{ block.shopify_attributes }}
>
  {%- if has_bg_image -%}
    {{
      block.settings.bg_image
      | image_url: width: 1920
      | image_tag:
        class: 'block-group__bg-media',
        loading: 'lazy',
        alt: '',
        widths: '375, 750, 1100, 1500, 1920',
        sizes: '100vw'
    }}
  {%- endif -%}

  {%- if has_overlay -%}
    <div
      class="block-group__overlay"
      aria-hidden="true"
      style="background-color: {{ block.settings.overlay_color }}; opacity: {{ overlay_opacity }};"
    ></div>
  {%- endif -%}

  <div class="block-group__inner">
    {% content_for 'blocks' %}
  </div>

  {%- if has_link -%}
    <a
      class="block-group__link"
      href="{{ block.settings.block_link }}"
      {% if block.settings.open_in_new_tab %}
        target="_blank"
        rel="noopener noreferrer"
      {% endif %}
      aria-label="{{ block.settings.block_link }}"
    ></a>
  {%- endif -%}
</div>

{% schema %}
{
  "name": "t:blocks.group.name",
  "blocks": [{ "type": "@theme" }, { "type": "@app" }],
  "settings": [
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "direction",
      "label": "Direction",
      "options": [
        { "value": "vertical", "label": "Vertical" },
        { "value": "horizontal", "label": "Horizontal" }
      ],
      "default": "vertical"
    },
    {
      "type": "select",
      "id": "alignment",
      "label": "Alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ],
      "default": "left"
    },
    {
      "type": "select",
      "id": "position",
      "label": "Position",
      "options": [
        { "value": "top", "label": "Top" },
        { "value": "center", "label": "Center" },
        { "value": "bottom", "label": "Bottom" },
        { "value": "space-between", "label": "Space between" }
      ],
      "default": "center"
    },
    {
      "type": "range",
      "id": "gap",
      "label": "Gap",
      "min": 0,
      "max": 80,
      "step": 2,
      "default": 16,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "Size"
    },
    {
      "type": "select",
      "id": "width",
      "label": "Width",
      "options": [
        { "value": "fit", "label": "Fit" },
        { "value": "fill", "label": "Fill" },
        { "value": "custom", "label": "Custom" }
      ],
      "default": "fill"
    },
    {
      "type": "number",
      "id": "custom_width",
      "label": "Custom width (px)",
      "default": 400
    },
    {
      "type": "select",
      "id": "mobile_width",
      "label": "Mobile width",
      "options": [
        { "value": "fit", "label": "Fit" },
        { "value": "fill", "label": "Fill" },
        { "value": "custom", "label": "Custom" }
      ],
      "default": "fill"
    },
    {
      "type": "number",
      "id": "custom_mobile_width",
      "label": "Custom mobile width (px)",
      "default": 300
    },
    {
      "type": "select",
      "id": "height",
      "label": "Height",
      "options": [
        { "value": "fit", "label": "Fit" },
        { "value": "fill", "label": "Fill" },
        { "value": "custom", "label": "Custom" }
      ],
      "default": "fit"
    },
    {
      "type": "number",
      "id": "custom_height",
      "label": "Custom height (px)",
      "default": 300
    },
    {
      "type": "header",
      "content": "Appearance"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "select",
      "id": "bg_media_type",
      "label": "Background media",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "image", "label": "Image" }
      ],
      "default": "none"
    },
    {
      "type": "image_picker",
      "id": "bg_image",
      "label": "Background image"
    },
    {
      "type": "select",
      "id": "border",
      "label": "Borders",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "solid", "label": "Solid" }
      ],
      "default": "none"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "Border color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "corner_radius",
      "label": "Corner radius",
      "min": 0,
      "max": 50,
      "step": 1,
      "default": 0,
      "unit": "px"
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "Background overlay color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay opacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "default": 0,
      "unit": "%"
    },
    {
      "type": "header",
      "content": "Block link"
    },
    {
      "type": "url",
      "id": "block_link",
      "label": "Link"
    },
    {
      "type": "checkbox",
      "id": "open_in_new_tab",
      "label": "Open link in new tab",
      "default": false
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top",
      "min": 0,
      "max": 100,
      "step": 2,
      "default": 0,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom",
      "min": 0,
      "max": 100,
      "step": 2,
      "default": 0,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_left",
      "label": "Left",
      "min": 0,
      "max": 100,
      "step": 2,
      "default": 0,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_right",
      "label": "Right",
      "min": 0,
      "max": 100,
      "step": 2,
      "default": 0,
      "unit": "px"
    }
  ],
  "presets": [{ "name": "t:blocks.group.presets.name" }]
}
{% endschema %}
